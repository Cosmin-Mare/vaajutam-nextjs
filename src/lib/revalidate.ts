/** Revalidate interval in seconds (ISR) for list/detail pages that read the DB. */
export const LIST_REVALIDATE = 60;

/** Static HTML fragments, partners page, and similar. */
export const FRAGMENT_REVALIDATE = 300;

/** Team / about page. */
export const MEMBERS_REVALIDATE = 120;
